$schema: 'https://moonrepo.dev/schemas/project.json'

tasks:
    lint-ts:
        command: 'pnpm lint-ts'
        inputs:
            - 'src/**/*.mjs'
            - 'eslint.config.mjs'
            - '/eslint.config.mjs'
