import { BaseIconHarness } from './base-icon.harness';

/** Test harness for the check icon component. */
export class CheckIconHarness extends BaseIconHarness {
    /** CDK host selector targeting `<dma-icon-check>` elements. */
    public static readonly hostSelector = 'dma-icon-check';
}
