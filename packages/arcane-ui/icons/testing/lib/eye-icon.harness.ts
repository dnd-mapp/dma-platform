import { BaseIconHarness } from './base-icon.harness';

/** Test harness for the eye icon component. */
export class EyeIconHarness extends BaseIconHarness {
    /** CDK host selector targeting `<dma-icon-eye>` elements. */
    public static readonly hostSelector = 'dma-icon-eye';
}
