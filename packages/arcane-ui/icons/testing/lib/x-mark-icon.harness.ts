import { BaseIconHarness } from './base-icon.harness';

/** Test harness for the x-mark icon component. */
export class XMarkIconHarness extends BaseIconHarness {
    /** CDK host selector targeting `<dma-icon-x-mark>` elements. */
    public static readonly hostSelector = 'dma-icon-x-mark';
}
