name: Pull request
on:
    pull_request:
permissions:
    contents: read
    actions: read
jobs:
    ci:
        name: CI
        runs-on: ubuntu-22.04
        steps:
            - name: Checkout repository
              uses: actions/checkout@v6
              with:
                  fetch-depth: 0
                  filter: tree:0

            - name: CI
              uses: ./.github/actions/ci
