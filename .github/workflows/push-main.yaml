name: Push to main
on:
    push:
        branches:
            - main
permissions:
    contents: read
    actions: read
jobs:
    ci:
        name: CI
        runs-on: ubuntu-22.04
        steps:
            - name: CI
              uses: ./.github/actions/ci
