# -----------------------------------------------------------------------------
# Handle line endings (Auto-detect text files and normalize to LF)
# -----------------------------------------------------------------------------
*                   text=auto eol=lf

# -----------------------------------------------------------------------------
# Explicitly mark specific file types as text to force LF (crucial for scripts)
# -----------------------------------------------------------------------------
*.ts                text
*.cts               text
*.mts               text
*.js                text
*.cjs               text
*.mjs               text
*.json              text
*.xml               text
*.md                text
*.html              text
*.svg               text
*.scss              text
*.css               text
*.yaml              text
*.yml               text
*.prisma            text
*.dockerignore      text
*.conf              text
Dockerfile          text

.gitignore          text
.prettierignore     text
.prettierrc         text
.tool-versions      text
.editorconfig       text
LICENSE             text

# Environment Templates (Always text, even with multiple dots)
.env*               text
*.template          text

# Shell scripts must always use LF to prevent execution errors on Linux/Docker
*.sh                text eol=lf

# -----------------------------------------------------------------------------
# Binary files (Prevent Git from trying to "fix" line endings in these)
# -----------------------------------------------------------------------------
*.png               binary
*.jpg               binary
*.jpeg              binary
*.gif               binary
*.ico               binary
*.pdf               binary
*.woff              binary
*.woff2             binary

# -----------------------------------------------------------------------------
# Workspace-specific ignore from GitHub statistics
# (Prevents auto-generated or vendored files from skewing language stats)
# -----------------------------------------------------------------------------
**/dist/*           linguist-vendored
**/node_modules/*   linguist-vendored
.idea/*             linguist-vendored
.run/*              linguist-vendored
.vscode/*           linguist-vendored
LICENSE             linguist-vendored
pnpm-lock.yaml      linguist-generated

# -----------------------------------------------------------------------------
# Diff Customization
# -----------------------------------------------------------------------------
# Treat lockfiles as binary to keep PR diffs readable
pnpm-lock.yaml      -diff
