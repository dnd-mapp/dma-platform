{
    "$schema": "../../node_modules/nx/schemas/project-schema.json",
    "name": "auth-server",
    "projectType": "application",
    "sourceRoot": "apps/auth-server/src",
    "tags": ["platform:nestjs", "scope:auth-backend"],
    "targets": {
        "prisma-generate": {
            "executor": "nx:run-commands",
            "inputs": ["{projectRoot}/prisma/schema.prisma", "{projectRoot}/prisma.config.ts"],
            "outputs": ["{projectRoot}/src/prisma"],
            "cache": true,
            "options": {
                "command": "prisma generate",
                "cwd": "{projectRoot}"
            }
        },
        "build": {
            "executor": "nx:run-commands",
            "outputs": ["{workspaceRoot}/dist/{projectRoot}"],
            "cache": true,
            "options": {
                "command": "webpack-cli build",
                "cwd": "{projectRoot}"
            },
            "defaultConfiguration": "production",
            "configurations": {
                "production": {
                    "args": ["--node-env production"]
                },
                "development": {
                    "args": ["--node-env development"]
                }
            }
        },
        "serve": {
            "executor": "@nx/js:node",
            "continuous": true,
            "options": {
                "buildTarget": "auth-server:build:development",
                "inspect": false,
                "runBuildTargetDependencies": false
            }
        },
        "test": {
            "executor": "@nx/jest:jest",
            "outputs": ["{workspaceRoot}/reports/{projectRoot}"],
            "cache": true,
            "options": {
                "jestConfig": "apps/auth-server/jest.config.cts"
            },
            "defaultConfiguration": "ci",
            "configurations": {
                "ci": {},
                "development": {
                    "watch": true
                }
            }
        }
    }
}
